const mainRoute = require("./main/main");
// const signUpRoute = require("./users/sign-up-route");
const products = require("./products-routes/products-router");

const router = {
  products
};

module.exports = router;
